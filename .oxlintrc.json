{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["typescript"],
  "env": {
    "browser": true,
    "node": true,
    "es2024": true
  }
  // No need for "ignorePatterns":
  // Oxlint automatically ignores files matched by `.gitignore`.
}
